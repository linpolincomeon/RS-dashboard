# CLAUDE.md — RS-dashboard (TomEnergy)

Contexto obligatorio para trabajar en este repo. Léelo completo antes de tocar código.
Última actualización: julio 2026. Al cerrar una sesión con cambios relevantes, actualiza la sección "Estado actual".

## Qué es este proyecto

Dashboards estáticos para TomEnergy (distribuidor de diésel B2B, Chile — RM, VI y VII Región).
Pipeline: scripts Python (XML-RPC contra Odoo 18 Enterprise) → JSON estáticos → HTML + Chart.js servidos por GitHub Pages.
Owner: Pauline Vial Comber (CEO). Trabaja directamente en el código, sin developer intermedio.
Comunicación siempre en español. Respuestas directas, sin relleno.

- Repo: `linpolincomeon/RS-dashboard` (público)
- URL live: https://linpolincomeon.github.io/RS-dashboard/
- Instancia Odoo: https://tomenergy.cl · DB: `PRODUCCION`

## Stack y archivos

| Script | Output | Consumidor |
|---|---|---|
| `extract_crm.py` | `crm-data.json` | `crm-sales.html` (dashboard PRINCIPAL) |
| `extract_ceo.py` | `ceo-data.json`, `riesgo-historico.json` | `ceo-dashboard.html` |
| `extract_route.py` | `route-data.json` | `route.html` |
| `odoo_mover_en_riesgo.py` | `riesgo-snapshots.json` | tab Riesgo |
| `extract_costos.py` | `costos-data.json` | `costos.html` (centros de costo camión) |
| — | `avla-lines.json` (manual, desde portal AVLA) | cobertura CxC en CEO dashboard |
| — | `vendor-goals.json` (manual) | metas por ejecutivo |
| — | `presupuesto-camiones.json` (manual, desde planillas Drive) | presupuesto en `costos.html` |

Otros: `crm-weekly.html` (Pipeline/Funnel), `board-dashboard.html` (standalone SheetJS, lee Excel local vía file picker, no depende del pipeline).

`reporte_chofer_conectado.html`: formulario móvil para choferes (reporte de bodega: inicio/carga/venta/traslado/cierre por camión). **NO usa el pipeline Odoo.** Envía por `fetch` `mode:'no-cors'` + body JSON a un Google Apps Script webhook, que escribe en la planilla `Bodegas_por_Camion_v2` (Sheet ID `11UTBZyBicW3HJcwnPpxyK...`). ⚠ El webhook DEBE recibir JSON en el body (`e.postData.contents`); NO cambiar a form submit/iframe (rompe el `JSON.parse` del `doPost`). El `no-cors` no bloquea la escritura, solo impide leer la respuesta. URL del deployment activo: `.../AKfycbyEEI4ujT7...`. Payload debe incluir `otro_camion` (el script lo usa para el movimiento espejo de traslados con bodega OTRA).

Frontend sin framework. Fuente DM Sans. Background `#f7f6f3`, cards blancas, acento `#c8360e`.

## CI / GitHub Actions

- Único secret: `ODOO_KEY`. El resto son literales en el YAML (`ODOO_URL=https://tomenergy.cl`, `ODOO_DB=PRODUCCION`, `ODOO_USER=p@tomenergy.cl`).
- Regla: pasar `env` al **step**, nunca al nivel de job (sobreescribe secrets con strings vacíos).
- Crons escalonados por incidente OOM (jul 2026): 03:00, 03:30, 04:00 hora Chile. NO volver a alinearlos.
- Conflictos de Actions sobre JSON: `git pull --rebase -X theirs origin main`.

## Constantes y modelos Odoo

- Diésel B1: `product_id = 14` (`DIESEL_PRODUCT_ID`)
- ENAP `partner_id = 5667` · Adquim `partner_id = 15299`
- Cheques en Cartera (journal): `id = 114`
- Vendedor: `res.partner.user_id` — SIEMPRE. Nunca `invoice_user_id`.
- Margen por factura: `margin_zone` en `account.move` (decimal 0–1)
- Crédito: `tiene_credito`, `monto_credito`, `saldo_credito` en `res.partner`
- Custom: `x_litros_estimados`, `x_origen_oportunidad`, `group_consultek_id`, `is_volume_client`, `delivery_zone_id`
- SLA: `shipping_date` (sale.order) vs `invoice_date` de la 1ra factura posted donde `invoice_origin = sale.order.name`. Excluir órdenes de hoy y fechas pre-2020.

### ⚠ Convenciones críticas (no negociables)

1. **`crm.lead.expected_revenue` NO son pesos: son LITROS MENSUALES esperados del lead.** Decisión deliberada de Pauline. Toda lectura en dashboards se interpreta como litros/mes.
2. **Las NC (`out_refund`) se restan manualmente** — Odoo no las netea.
3. **Ejecutivo real ≠ `user_id`.** La gestión real se determina por quién registra actividad (`mail.message` sobre `res.partner` o `crm.lead`), no por la cuenta asignada.
4. **Semana comercial: JUEVES a MIÉRCOLES** (calendario ENAP). Aplica a todo.
5. Odoo 18 estampa `write_date`/`date_last_stage_update` iguales en batch del cron 07:30 — usar `stage_update` en `getLeadDate()`.

## Lógica comercial

- Segmentación: Retail = `is_volume_client` False (meta margen 8,5%) · Volumen = True (meta 6%). Metas confirmadas por Pauline 2026-08-13; son las del dashboard (`margen_contado_meta`/`margen_credito_meta`, fallbacks 0.085/0.06 en `ceo-dashboard.html`). Cliente "Predeterminado" excluido de todo. **Regla volumen (comité, 2026-08-13):** ≥20.000 L en algún mes de los últimos 6m, o ≥5.000 L en un despacho (factura) de los últimos 6m, o proyección anual ≥216.000 L (12×18.000, anualizada si tiene <12m de historia). La calcula el paso 7 de la acción Odoo 1243 (el "Promedio Anual" del módulo quedó fuera: cron 88 desactivado).
- Margen %: `(Venta_neta − Costo_neto)/Venta_neta × 100`; `Costo_neto = Venta_neta × (1 − margin_zone)`
- Precio neto: `(bruto − IEC)/(1+IVA)` · IEC = $104.5749/lt · IVA = 19%
- Churn %: perdidos nuevos del mes / clientes activos mes anterior. Perdido = 9 meses (270 días) sin factura posted.
- Durmancia dinámica: freq <30d → durmiente si supera freq×1.5 sin comprar; freq ≥30d → freq×1.3.
- Rescate Durmiente → Fidelización (Comber Sigall). Rescate Perdido → ejecutivo comercial, cuenta como cliente NUEVO.
- **Cliente nuevo queda con su ejecutivo 180 días** (antes 90; cambiado 2026-07-23, alineado al modelo de comisiones de 6 meses). Vive en DOS lados: (a) dashboard `extract_crm.py` graduating (ventana 150–210d, pendiente ≥180) y (b) Odoo acción de servidor id 1243 "Actualizar Días Primera Facturación y Categoría Cliente" (cron 84 diario 02:12): `nuevo`→`activo` a los 180d si `user_id ∈ ejecutivos_ids`, 90d para el resto. `ejecutivos_ids` = SOLO las cuentas `.ext` (10). ⚠ Las cuentas antiguas de Toro (56, sebastian@) y Muñoz (262, joaquin@) son la modalidad FULL-TIME con comisiones ya pagadas: sus clientes NO se tocan y siguen en 90d — los dos usuarios existen justamente para no mezclar modalidades. Al agregar un ejecutivo nuevo hay que sumar su `.ext` AHÍ también (además de `_CANONICAL_VENDORS`/`FREELANCERS`).
- Riesgo crediticio (score 0–100, mayor = peor): Morosidad 40 · Utilización 25 · Cobranza/Siniestro 20 · Margen 15. Crítico ≥60, Alto ≥40, Medio ≥20. Consolidación por grupo vía `group_consultek_id`.
- Cobertura AVLA: por RUT, `min(deuda_cliente, cobertura_efectiva_avla_clp)`. Cobertura efectiva = `monto_aprobado_UF × uf_value × (cobertura%/100)`. ⚠ El % (80% innominados / 90% nominados) se aplica **desde 2026-07-08**; los avla-lines.json previos usaban 100% nominal (ver Estado actual).
- Calculadora de excepción: `minimo = base_min_zona + (−dias_real + 30) × C10 / 30`; `excepcion = minimo − 10`; C10 = −16. Usa días REALES (`avg_payment_days`). **Desde ago-2026 todo en NETO:** `base_min_zona = ENAP_neto/(1−margen_min)` con Crédito 7,5% · Volumen 5,5% · Contado 6,5% (ver Tabla de zonas Mantenedor).

## Equipo activo (junio 2026 — `ALLOWED_VENDORS`)

Formales: Joaquín (Muñoz Encalada), Comber Sigall Pauline (fidelización/CS; meta mantención cambia mensualmente). Carlos Labbe también cuenta como ejecutivo formal en la KPI "Litros Ejecutivos".
Manuel Santana: **removido de los dashboards el 2026-07-10** (quitado de `ALLOWED_VENDORS`, `EJECUTIVOS`, `EXEC_COUNT` 4→3 y canonical de `extract_crm.py`). No tenía facturas de julio, sin impacto en números del mes.
Freelancers: Sebastián Toro (sin meta), Cristian Jiroz, Manuel López Allende, Carolina Avilés, Diego Varas, Marcela Márquez, Raúl Bisquertt, Nicolás Gonzalez, Rodrigo Retamal.
Removidos de TODA referencia: Fernando Jullian, Yeniré Ron, André De Trenqualye, Vanessa Vázquez, Julio Phillipi, Turner Fabres Antonio.
Las dos Paulines: **Vial Comber** = CEO (NO aparece en dashboards de ventas) · **Comber Sigall** = madre, fidelización (sí aparece en tab CS).

## Reglas de código

- Ediciones quirúrgicas: diff mínimo sobre el archivo existente. No reescribir archivos completos salvo pedido explícito.
- Nombres canónicos: `canonical_vendedor()` (Python) / `canonicalName()` (JS). `safe_id()`/`safe_name()` para Many2one con `False`.
- `sr()` firma: `sr(models, uid, model, domain, fields, limit=, order=)` — db/key son globales.
- `limit=500` trunca silenciosamente. Usar 5000–20000.
- Dominio Odoo = lista de triplets: `[["move_type","=","out_invoice"]] + base_domain`.
- Verificar nombres de campos contra el JSON live antes de referenciarlos (ej. `w.litros` vs `w.value`).
- Validación antes de commit: `python3 -m py_compile` en scripts + checks estructurales en HTML (balance de divs).
- Commits: mensaje descriptivo en español, un tema por commit. Nunca force push a main. Confirmar con Pauline antes de push si el cambio toca lógica de negocio (fórmulas, filtros de vendedores, definiciones de churn).
- Pages tiene lag de deploy: verificar cambios vía `raw.githubusercontent.com`, no la URL live.

## ⚠ Bugs abiertos / zonas prohibidas

- ✅ **DSO en `extract_ceo.py`: CORREGIDO con `amount_residual` + `account.partial.reconcile`, validado 2026-08-03.** Se reescribió el saldo CxC: actual = Σ `amount_residual` de líneas posted; cierres históricos = residual de líneas ≤ fecha + reversa de reconciliaciones con `max_date` posterior al corte. Validación paralela 6 meses: **ambos métodos coinciden peso por peso ($0 de diferencia en 8 cierres)** — el "bug" documentado no se reproducía a nivel agregado; el método nuevo queda igual por ser correcto por construcción. ⚠ Hallazgo de la validación: los DSO presentados al directorio derivan con el tiempo por contabilidad tardía (jun-26 presentado 28,0 → real 33,3) — la deriva es movimiento contable posterior, NO error de método. Además `validate_output()` al final del script: DSO fuera de [5,90]d o CxC hoy ±50% vs el último `ceo-data.json` → exit 1 sin escribir el JSON (el cron de Actions avisa por email en vez de publicar un dato implausible). Se eliminó también el fallback hardcodeado de la key revocada de julio.
- **Bloque de auto-actualización AVLA en `extract_ceo.py` está DESHABILITADO a propósito** (generaba entradas falsas en `riesgo-historico.json`, ej. 14/06 con 100% no cubierto por mismatch de filename `Avla Lines` vs `avla-lines.json`). No reactivar sin instrucción explícita.
- `dias_facturacion` en `res.partner` está roto (probablemente usa `write_date`). Calcular días desde `account.move`.

## Tabla de zonas Mantenedor (hardcoded en ceo-dashboard.html)

⚠ **Desde ago-2026 el Mantenedor trabaja en NETOS (sin IVA/IEC)** — antes la tabla estaba en brutos. En el HTML solo se hardcodean `bomba`/`enap` por zona (`ZONAS_NETO`); los mínimos se calculan como `ENAP/(1−margen_min)` con Crédito 7,5% · Volumen 5,5% · Contado 6,5%. Actualizar aquí Y en el HTML cuando ENAP cambie precios. Valores de agosto 2026 — verificar vigencia contra el Sheet Mantenedor antes de usar.

| Zona | Bomba (neto) | ENAP (neto) |
|---|---|---|
| Rancagua | 1254 | 1092 |
| San Fernando | 1244 | 1092 |
| VI Costa | 1219 | 1092 |
| Talca / Curicó / Chillán | 1195 | 1081 |
| Región Metropolitana | 1244 | 1092 |

RM no tiene fila propia en el Mantenedor: "Paine y Lampa se cotizan a precio San Fernando" (nota de la hoja) → RM usa San Fernando.

## Documentación adicional

Si la tarea toca recaudación, cash flow, calculadora de excepción o debugging histórico: leer `REFERENCIA.md` (raíz del repo) antes de escribir código.

## Estado actual (julio 2026)

- `crm-sales.html`: funcionando. Tab Recuperación completado, columna Actividad live.
- SLA reescrito y funcionando (join `invoice_origin`), respeta selector de semana.
- Board dashboard operativo (histórico 2011–2026).
- Churn del CEO dashboard alineado a definiciones correctas.
- **AVLA (2026-07-08):** `avla-lines.json` regenerado desde export `INSURED_LINES` del portal (descarga 08/07/2026, UF 40842.07). Filtro: solo líneas `Actual` + estado `Aprobada`/`Aprobada parcialmente` → 706 RUTs. **Cambio de metodología:** ahora se aplica el % de cobertura (80/90) → cobertura total $6.920M (vs $6.683M en junio a 100%). ⚠ Esto crea una **discontinuidad en `riesgo-historico.json`**: los snapshots ≤ jun-2026 usan cobertura nominal al 100% y NO son comparables directos con jul-2026 en adelante. No re-escribir el histórico previo (los valores viejos eran correctos para su metodología); al comparar tendencia de "% no cubierto", tener presente el quiebre en esta fecha.
- **AVLA refresh (2026-08-04):** `avla-lines.json` regenerado desde export `INSURED_LINES` del 03-08-2026 (UF 40844.79, mindicador.cl). 735 RUTs / $7.977M cobertura (vs 706 / $6.920M en julio): +34 RUTs nuevos, −5 salieron, y aumentos grandes de línea en SUGAL (UF 13.000) y Frutícola Agrichile. **Cambio menor de filtro:** ahora se incluye también el estado `Aprobada reducida` (nuevo en este export; eran las 2 MARVAL, reducidas de UF 3.000→1.500 — excluirlas las dejaba en $0 siendo cobertura vigente). Verificado contra Odoo live antes de commitear: % monto no cubierto pasa de 15,6%→11,1%, match 127/143 clientes con deuda. La auto-actualización AVLA sigue deshabilitada a propósito; el refresh es manual con export del portal.
- **Santana + meta Julio (2026-07-10):** eliminado Manuel Santana de todos los dashboards (ver sección Equipo activo). `EXEC_COUNT` 4→3 en `crm-sales.html` y `crm-weekly.html` (metas de equipo del funnel se escalan sobre 3 ejecutivos). **Fix meta mensual:** `company_goals.litros_mes`/`month` estaban hardcodeados en junio (866.750) y no se bumpeaban al cambiar de mes; ahora son dinámicos vía `_meta_mes(today)` + mes actual sobre `BUDGET_2026`. Julio muestra 1.107.706. Auto-mantenido de aquí en adelante.
- **RUT representante legal (2026-07-17):** el campo Studio `x_studio_rut_representante_legal` en `res.partner` era requerido para toda empresa (vista Studio id 3616), lo que bloqueaba guardar fichas de clientes antiguos sin el dato. Cambiado vía XML-RPC a `required="is_company and not id"`: obligatorio solo al CREAR una empresa nueva; las existentes se editan sin restricción. Cambio hecho directo en Odoo (no vive en este repo).
- **Fix "sin gestión" (2026-07-17):** `gather_latest_note()` solo miraba `message_type in ['comment','note']`, pero **Odoo 18 registra las notas y actividades completadas como `message_type='notification'`** (subtipo Note/Activities) → descartaba toda la gestión de clientes ganados/sin actividad abierta (ej. INVINA, ECONUT salían "Sin gestión" teniéndola). Fix: filtrar por **subtipo** (Note + Activities) en vez de message_type, con `_clean()` que desenvuelve "Actividades pendientes hecho :"/"To-Do done :" y recorta "Feedback:", y lista de ruido ampliada (ganado automáticamente, facturas pendientes, cierre masivo, etc.). ⚠ Quitar el filtro sin batchear **revienta el server con OOM** (245 pids × todos los mensajes con body) — por eso va **batcheado (chunks de 100) + `limit=8000` + filtro de subtipo**. Impacto: Recuperación pasó de 2 "sin gestión" a 0; corrige subconteo global. Mismo ruido aplicado al `_noise` de notas won/pipeline.
- ✅ **Seguridad — key rotada (2026-07-17):** la `ODOO_KEY` que estaba expuesta en el historial de git (commit `90de552`) fue **revocada en Odoo** (verificado: ya no autentica). Secret `ODOO_KEY` en GitHub actualizado con key nueva y workflow verificado en verde. ⚠ **La key nueva VENCE el 12/01/2027** (creada con 6 meses): si en enero 2027 los crons empiezan a fallar con "Authentication failed", es esto — crear key nueva en Odoo y actualizar el secret. Los secrets sin uso (`ODOO_API_KEY`, `ODOO_API_KEY25MAR`, `ODOO_DB`, `ODOO_USER`) se eliminaron de GitHub; el único vigente es `ODOO_KEY`. Pendiente menor: activar 2FA en la cuenta Odoo de Pauline.
- **Modelo comisiones freelancers (2026-07-17):** en diseño. Simulación antes/después en `~/Desktop/Simulacion_comisiones_freelancers.xlsx` (datos Odoo al 17-jul, hojas Resumen/Clientes/Mensual/Parámetros con celdas editables). Contrato actual: tablas $/lt por diferencial venta-vs-bomba (Tabla 1 Central / Tabla 2 Otras, decodificadas del docx), solo 6 primeros meses por cliente captado, contrato máx 3 meses. Propuesta: mismas tablas meses 1–6 + cola 50% meses 7–12 + bono retención 90d + contrato sin tope de 3 meses. Identificación freelancers: login `*.ext@tomenergy.cl` (ojo: Garroz es `.ext` pero está en NÓMINA). Las bombas viven en `partner.delivery.zone` (`lst_price` neto; bruto = neto×1,19+IEC) **sin histórico** — calibración histórica solo contra boletas reales (BHE/FAC proveedor; Carolina may-26 calza exacto al peso). Detectado: Manuel López vende desde mayo sin boletas registradas; Toro factura como proveedor Y tuvo liquidación en nómina (may-26); key local para scripts en `~/.odoo_key`.
- **Tab Ejecutivos = solo freelancers (2026-07-20, corregido 2026-07-24):** la lista (`EJECUTIVOS_ALL`) es de freelancers; Toro y Muñoz se sacaron el 20-jul cuando eran full-time pero **volvieron el 24-jul como freelancers `.ext`** (fullName corto `sebastian toro`/`joaquin muñoz`, que además hace match con sus cuentas antiguas y muestra el consolidado). Incluye también a Abraham Urrutia (`abraham.ext@tomenergy.cl`, ingresó 14-jul, agregado también a `FREELANCERS` y a `_CANONICAL_VENDORS` del extract). El tab se filtra dinámicamente con `execHasMovement()`: sin movimiento (litros semanales, won, cliente nuevo o actividad CRM) en las últimas 4 semanas → no aparece el botón. ⚠ `pipeline` en el JSON viene truncado a 150 leads y el cron deja todos con `days_in_stage=0`, así que el filtro usa además `executives[].moved_30d` (agregado sobre TODOS los leads). NO se tocó `ALLOWED_VENDORS` ni `EXEC_COUNT` (KPIs de equipo siguen igual). Además: columna "Prom. Mensual" (litros) en "Clientes Perdidos Este Mes" (Fidelización), y en "Transición a Fidelización" la columna Facturas (n° órdenes) se reemplazó por Litros (`graduating[].litros` nuevo en `extract_crm.py`, suma de líneas diesel B1; `_exec_names` del graduating ampliado a todos los freelancers).
- **Regla 180 días (2026-07-23):** cliente nuevo queda con su ejecutivo externo 180 días, no 90 (ver sección Lógica comercial). En el repo: graduating de `extract_crm.py` con ventana 150–210d y labels de "Transición a Fidelización" en `crm-sales.html`; se agregaron los nombres cortos `sebastian toro`/`joaquin muñoz` a `_exec_names` (las cuentas `.ext` no canonicalizan al nombre largo y sus clientes quedaban fuera). En Odoo: acción 1243 actualizada — `ejecutivos_ids` ahora incluye joaquin.ext (284) y las cuentas antiguas de Toro (56) y Muñoz (262); 6 clientes que se habían volteado a `activo` con <180d fueron devueltos a `nuevo`. ⚠ La tabla Transición quedó vacía al momento del cambio: los clientes de 150–210d ya habían sido traspasados a CS bajo la regla de 90 — se repobla sola con las cohortes nuevas. ⚠ Hubo un ida y vuelta que terminó REVERTIDO el 24-jul: 49 clientes 90–179d que estaban en CS se devolvieron primero a Toro (cuenta antigua, luego sebastian.ext) para comisión, pero Pauline aclaró que eran **capturas de la época FULL-TIME (cuenta antigua) ya pagadas y ya traspasadas a CS** → los 49 volvieron a su usuario original (45 TomEnergy, 4 Comber) y a categoría `activo`. Regla final: los clientes capturados bajo cuentas antiguas NO se tocan aunque tengan <180d; la ventana de 180d aplica solo a capturas hechas con cuentas `.ext`. Carlos Labbe (user 77, sin `.ext`) NO está en `ejecutivos_ids` — decisión de Pauline: es jefe, no recibe comisión directa.
- **Cotizaciones solo humanas + envío automático apagado (2026-07-24):** los crons Odoo 66/67/68 (`generate_so` semanal/mensual/anual) crean ~970 borradores de sale.order al mes bajo OdooBot (uid 1), y la automatización base.automation id 1 "Cotización creada" los ENVIABA por correo al cliente (dominio `create_uid.id = 1` + lista manual de partners excluidos). Auditoría de precios: solo 18% de esos borradores tenía precio diésel dentro de ±2% del precio de zona vigente (53% iba >5% sobre precio, varios en $0). Decisión de Pauline: **automatización 1 DESACTIVADA** (nadie recibe correos automáticos); los crons generate_so siguen activos como insumo interno. En el dashboard, `quote_domain` de `extract_crm.py` ahora excluye `create_uid = 1`: el funnel/KPI Cotizaciones cuenta solo cotizaciones creadas por personas (semana 16-22 jul: 586→72). Si se quiere reactivar el envío, la opción diseñada es enviar solo cotizaciones con precio dentro de ±2% de zona (no implementado).
- **KPI Litros redefinido (2026-07-24):** "Litros Ejecutivos" = vendedores INTERNOS (todo lo que no es freelance ni CS: Labbe, Isabel, etc., calculado por residuo con `!isFreelancer && !isCS`); "Litros Freelancer" = todos los `.ext` incluyendo Toro y Muñoz (agregados a `FREELANCERS` con nombre corto, hace match con sus dos cuentas). Aplica a tab Ventas y Mes Vencido; glosario actualizado. `ALLOWED_VENDORS` NO se tocó (sigue alimentando ranking/hot deals/pipeline).
- **Poda de redundancia (2026-07-24):** auditoría de utilidad/repetición del dashboard. Eliminado: `renderRiesgo()` + `loadRiesgoHist()` (código MUERTO, ~115 líneas — no tenía tab; el riesgo vive en el CEO dashboard), Hot Deals duplicada del sub-tab Acumulado de Ventas, y del tab Pipeline todo lo contaminado por el re-estampado batch de fechas del cron de Odoo (Velocidad Promedio, Movimientos Semana, Necesitan Acción/Atención, Backlog Legacy, Ranking Ejecutivos por movimientos, $ por etapa del Flujo — el JSON pipeline viene truncado a 150 leads y los montos salían parciales). El tab Pipeline queda: conteos por etapa (KPIs + Flujo), Hot Deals, Movimientos Recientes, Actividades/Notas. **Bug de fondo corregido en `extract_crm.py`:** `funnel` contaba por `stage_class` y las etapas que comparten clase (Oportunidad de Negocio / En Riesgo / No Cerrados) mostraban TODAS el total de la clase (505); ahora cuenta por nombre de etapa. Los 3 tabs de fuga/rescate se fusionaron en 2 el 24-jul con feedback del equipo (ver entrada siguiente).
- **Gestión CS solo interna + Litros/lista Rescatados (2026-07-24):** los tabs Churn & Rescate y Fidelización+CS muestran SOLO gestión del equipo interno no-freelance — lista `CS_GESTION` en `crm-sales.html` (Comber, Mauro, TomEnergy, Isabel Muñoz, Tomás Vidal, Madelaine Toledo); la gestión de ejecutivos de venta/freelancers no aparece ahí. El split de litros NO cambió (`CS_TEAM` sigue siendo Comber+Mauro+TomEnergy; los litros de Isabel siguen en "Litros Ejecutivos" internos). Además: KPI "📦 Litros Rescatados" y tabla "Clientes Rescatados" (origen Durmiente/Perdido) en tab CS; `rescued_dormant_list`/`rescued_lost_list` del extract ahora traen `avg_monthly_litros` (8 meses diesel neto NC). Ganado = oportunidad Won del CRM; Rescatado = durmiente/perdido que volvió a facturar este mes.
- **Reestructura fuga/rescate en 2 tabs + FECHAS REALES de etapa (2026-07-24, feedback reunión de equipo):** los tabs Churn & Rescate / Fidelización+CS / Recuperación se fusionaron en DOS según diseño de Pauline: **"🔁 Gestión Comercial Recuperación"** (KPIs de cartera + Nuevos Perdidos del Mes + Transición a Fidelización — lo gestiona ventas, SIN filtro CS) y **"👩‍💼 Gestión ADMIN Fidelización"** (KPIs CS + Clientes Ganados + Clientes Recuperados + Nuevos Durmientes + Estacionales; Recuperados/Ganados con filtro `CS_GESTION`, Durmientes sin filtro porque es la cola de trabajo de admin). **Fix de fondo en `extract_crm.py`:** las tablas "nuevos durmientes/perdidos de la semana" mostraban SIEMPRE lo mismo porque el cron de Odoo re-estampa `write_date` en batch — ahora `last_update` de dormant/lost/rescued lists es la fecha REAL del cambio de etapa vía `mail.tracking.value` (cobertura ~860/895 leads; fallback write_date). **Mismo fix aplicado a `won_deals`** (won_date/stage_update reales, 12/30 con tracking): antes todas las semanas pasadas mostraban 0 Clientes Ganados en CS porque los Won quedaban apilados en la fecha del re-estampado — era el "CS had nothing" de la reunión del 24-jul. También se eliminó el KPI "En Riesgo" del tab Ventas (huérfano tras quitar la tabla; el riesgo vive en el CEO dashboard). Con fechas reales los rescates del mes pasaron de 5 a 20 durmientes (42.170 L). ELIMINADO en la fusión: tabla "Clientes En Riesgo Crediticio" (el riesgo vive en CEO dashboard), "Todos los Durmientes" y `renderRecovery()`. La tabla **"Recuperables ahora" se RESTAURÓ el mismo día** en el tab comercial a pedido de Pauline: 21 de 28 recuperables no aparecen en Perdidos/Durmientes (detección por caída de facturación, varios sin lead CRM) — NO es redundante con las tablas CRM. La "Tasa Rescate" del KPI usa `rescued_dormant+rescued_lost` (antes mostraba "0 rescatados" por campo inexistente).
- **Candado cambio de vendedor + auditoría Toro (2026-07-24):** tras detectar que Toro movía clientes antiguos/perdidos de la cartera (TomEnergy/Comber y su cuenta full-time) hacia su `.ext` para regenerar comisiones ya pagadas (24 movimientos auditados vía `mail.tracking.value`; 12 con primera factura pre-13-may, detalle en `~/Desktop/Auditoria_cuentas_Toro_24jul.xlsx`), se creó en Odoo la automatización **base.automation id 21 "Bloquear cambio de vendedor no autorizado"** (server action 1296): solo Madelaine (50), Pauline Vial (53) y Carlos Labbe (77) —más OdooBot(1)/admin(2) para crons— pueden modificar `user_id` de un partner con `customer_rank > 0`; el resto recibe UserError y el cambio se aborta. Prospectos sin facturación y contactos recién creados (<10s) NO se bloquean para no romper la captura normal. Probado end-to-end. Regla de Toro: salió de full-time el 13-may-2026, comisión de cuenta antigua máximo hasta 13-ago. Los clientes que él ya había movido a `.ext` fueron gestionados directamente por Pauline/equipo el mismo 24-jul (resuelto, no requiere acción).
- **Tab Operaciones (2026-07-30):** nuevo tab "🚚 Operaciones" en `crm-sales.html`: SLA de entrega del MES ACTUAL (mismo método `extract_sla_data`: shipping_date vs 1ª factura por `invoice_origin`) + litros entregados por camión por día (30 días). Camión = `stock.warehouse` de la sale.order (HHPT-71, PHXC-44, PYHK-28, SHGP-60, TJVS-53, TYDG-23, VDKT-95); litros desde facturas posted → join `invoice_origin`. Nuevo bloque `operaciones` en `extract_crm.py` (`extract_operaciones`). **Métrica principal (definición FINAL de Pauline 31-jul, tras iterar): "SLA Entrega 24h" = pedido facturado a más tardar el día siguiente de ponerse; si es preorder con `shipping_date` posterior, vale esa fecha como plazo; meta 95%** — `sla_semanas[].prom_*` en el JSON: TODOS los pedidos humanos de la semana (hora Chile; Odoo guarda UTC, invierno = UTC-4), cumplido = 1ª factura ≤ plazo; plazo abierto = "en reparto" sin evaluar. Tabla "⏰ Fuera de Plazo" con hora del pedido, plazo y factura. Julio: 97–100% por semana (la variante "mismo día pre-11 AM" daba 71–83% y se descartó por compleja). Secundarios: "vs fecha sugerida" (facturadas >1d después de `shipping_date` — OJO: ese campo se fija cuando el despacho ya está resuelto y sale ~100% solo, auditado 30-jul, no usarlo como SLA principal) y matrices de litros diarios por camión y POR ZONA (`zonas_diario`, delivery_zone del cliente). Sin gráficos (pedido de Pauline: solo las 2 matrices). Historia de iteración: se descartaron dos métricas inventadas (SLA shipping_date ~100% tautológico y lead time ≤48h desde el pedido, injusto con entregas programadas a futuro) antes de llegar a la promesa real — preguntar SIEMPRE cuál es la promesa del negocio antes de definir un SLA.
- **Remate de clientes + tabla Asignaciones (2026-07-31):** el comité asigna clientes perdidos a ejecutivos ("remate"). Criterios de la lista (Pauline): SOLO etapa perdidos (no durmientes), sin compra reciente, y LEER la última nota — si dice "compra por otra razón social" se excluye (ej. El Arrayán compra por Hacienda La Sorpresa; Reyes y Reyes por Patricio Reyes). Nueva tabla "🎯 Clientes Asignados a Ejecutivos" en el tab comercial con selector de semana ENAP: `asignaciones` en el JSON (`extract_asignaciones` vía `mail.tracking.value`, cambios de vendedor hechos por Madelaine/Pauline Vial/Carlos hacia cuentas `.ext`, solo VIGENTES — las revertidas no aparecen) con check ✅/❌ de gestión posterior a la asignación, para revisar en el comité siguiente. Además: columna Zona en "Nuevos Perdidos del Mes" (`lost_list[].zona`) y `sr()`/`s_count()` con reintentos ante 502 de Odoo (pasaba en horario de reunión con todo el equipo conectado).
- **Monitoreo de calidad de datos (2026-08-03):** nuevo `validate_data.py` — corre en los 3 workflows activos entre el extract y el commit; si el dato es implausible el job falla a propósito (exit 1), NO se commitea (el dashboard conserva el último dato bueno) y llega el email de falla de GitHub. Checks: JSON válido, frescura ≤26h, encogimiento <50% vs HEAD, y por archivo — CRM: última semana ENAP **cerrada** ≥50k litros (los acumulados del mes pueden ser 0 legítimos a inicio de mes, ver c47303e), mes anterior ≥300k, ≥700 activos, ≥5 ejecutivos, funnel ≥5 etapas/100 leads, bloques churn/recovery/operaciones/pipeline presentes; CEO: última semana `parcial=False` ≥50k litros, total_cash>0, ≥3 bancos, CxC ≥$50M/≥40 facturas, AVLA cubierto+no_cubierto>0; riesgo-historico: append-only (jamás se achica); route: solo estructura+frescura (día sin pedidos es legítimo). Umbrales editables al tope del script (~mitad del peor histórico). ⚠ Descubierto en la misma sesión: los `update-*.yml` de la RAÍZ del repo son copias inertes (el activo vive en `.github/workflows/`) — el commit 90140c7 que movía el cron CRM a 10:00 UTC editó la copia muerta; se aplicó al workflow real (08:00→10:00 UTC, evita mantención Odoo ~05:50). `update-data.yml` (extract_odoo legacy) está `disabled_manually` en GitHub — por eso data.json quedó congelado en marzo; sin acción.
- **Ventana "nuevo" por CICLO de facturación (2026-08-04):** la acción de servidor 1243 volteaba `nuevo`→`activo` usando `dias_primera_facturacion` = días desde la PRIMERA factura de la historia, lo que quemaba la ventana de 180d de los rescates de perdidos y de cualquier cliente con facturas históricas migradas (carga masiva del 30-sep-2025, `create_date` de facturas 2021–2024 = 30-sep-2025). Ej.: GESTION Y SERVICIOS SPA capturado por Carolina `.ext` el 8-jul-2026 quedaba `activo` la misma noche por una factura de abr-2023. **Fix en la acción 1243** (aplicado vía XML-RPC, probado end-to-end): la ventana 180/90 se cuenta desde la primera factura del **ciclo actual** = primera factura posterior al último gap ≥270 días sin facturar (misma definición de perdido) — SQL con `LAG()` sobre `account_move`. `dias_primera_facturacion` (el campo) NO cambió, sigue siendo primera factura ever. **Mismo fix en el graduating de `extract_crm.py`** (Transición a Fidelización usa ahora el inicio de ciclo). Se devolvieron a `nuevo` 5 rescates: GESTION Y SERVICIOS SPA (Carolina), TRANSPORTES VICTOR VALDIVIA (Nicolás), COMUNIDAD SAN AGUSTÍN DE TALCA, RENE ANTONIO GALLARDO URETA y VICTOR MANUEL GALLARDO URETA (Toro `.ext`). ⚠ AGRICOLA Y COMERCIAL SANTA MAGDALENA (Toro cuenta ANTIGUA, uid 56): su ciclo partió el 5-may-2026 y las cuentas antiguas siguen con regla de 90d → cumplió los 90 el 3-ago y quedó `activo` por regla; ponerlo en `nuevo` sería revertido por el cron salvo decisión explícita de Pauline (pendiente).
- **Durmancia unificada con umbral DINÁMICO + cubre a los "nuevo" (2026-08-04):** con la ventana de 180d un `nuevo` que dejaba de comprar quedaba invisible hasta 6 meses (las reglas durmiente/perdido solo miraban `activo`). Decisión de Pauline: se resuelve por la vía durmientes, sin categoría nueva. Además convivían DOS sistemas de durmancia que se pisaban: `customer_categ_activo` (viejo, 60d fijo, escrito por acción 1243, alimenta la cola vía automatización 9) y `sale_frequency_status` ("Durmiente 2.0", escrito por el módulo `cr_average_payment_days`, es lo que muestra la ficha vía `x_studio_durmiente` related) — 12 divergencias, 5 pegados del 2.0, y NADA reseteaba los flags al volver a comprar (41 clientes con compra reciente seguían marcados y sus leads pegados en etapa Durmiente). **Reescritura de reglas 4+5 en la acción 1243** (vía XML-RPC, probado end-to-end): umbral POR CLIENTE = `frecuencia_facturacion` ×1.5 (freq <30d) / ×1.3 (freq ≥30d) con **piso 7 días** (misma ecuación del cron 93 y de la durmancia dinámica del dashboard); sin frecuencia utilizable ("Irregular / Poca data" o vacía, ~75% de la cartera) → fallback 60d. Marca solo `activo`/`nuevo` (categoría y ejecutivo NO se tocan → comisión intacta); despierta a cualquiera dentro de su umbral; **escribe SIEMPRE los dos flags en conjunto** — la acción 1243 es el único escritor nocturno y la ficha y la cola ya no pueden divergir. Resultado 1ª corrida: 26 marcados (semanales/quincenales apagados que el 60d fijo no veía), 11 despertados, divergencias 0. Cadena Odoo: acción 1243 (cron 84, corre 02:12 Y 14:12) marca flags → auto 9 (lead → etapa Durmiente, id 5) → auto 10 (lead → Ganado al despertar); cron 93 = alertas por frecuencia al ejecutivo (todas las categorías); `dias_facturacion`/`frecuencia_facturacion` los computa el módulo (`_compute_invoice_days()`, auto 15). **Paso 6 agregado (2026-08-06):** las automatizaciones 9/10 son "por tiempo" sobre el lead y NO se re-disparan cuando el flag cambia por SQL (quedaron 10 leads rezagados en Won/En Riesgo/Ruta, corregidos a mano) — ahora la acción 1243 mueve las etapas ella misma (contexto sin notificaciones, igual que acción 1163; no toca etapas de perdidos/no cerrados). ⚠ Hueco conocido: ~89 de 291 durmientes NO tienen lead CRM (cartera antigua TomEnergy) → invisibles en las tablas CRM del dashboard ("Nuevos Durmientes" muestra top 50 por litros); solo los pesca "Recuperables ahora" por caída de facturación. Pendiente decisión de Pauline: si crear lead automático al marcar durmiente.
- **Calculadora de excepción en NETO (2026-08-13):** el equipo actualizó el Sheet Mantenedor (se perdía plata en venta de volumen). La fórmula de excepción NO cambió (`min + (−días+30)×C10/30`, C10=−16, excepción=min−10, verificado contra las fórmulas del xlsx exportado), pero la base sí: precios NETOS y mínimos = `ENAP/(1−margen_min)` con **Crédito 7,5% · Volumen 5,5% · Contado 6,5%** (antes tabla bruta con 7%/8%/4,5%). En `ceo-dashboard.html`: `ZONAS_PRECIOS` reemplazado por `ZONAS_NETO` (bomba/enap por zona) + `MARGEN_MIN`, mínimos calculados en código — próximo cambio ENAP solo requiere tocar bomba/enap. RM → precio San Fernando (nota "Paine y Lampa" de la hoja). Sanity checks nuevos validados contra la hoja: San Fdo/Crédito/30d → $1.171 B−73 · Talca/Crédito/30d → $1.159 B−36. Los diffs B±X ahora son netos y calzan con cómo cotizan los ejecutivos (hojas "X Calculadora" del mismo Sheet). Sheet: `1MKEHvkg3vrN1YDKz5sIaEjQDZ837NB-8s_zuFULoag8`.
- **Recuperación afinada (2026-08-12):** reglas de Pauline sobre "Recuperables asignables": fuera los con `quebr`/`siniestr` en nombre o nota; fuera los YA asignados a un ejecutivo `.ext` con gestión <3 meses (sin gestión 3+ meses vuelven al pool); lista **`RECUPERABLES_MANUAL`** en `extract_crm.py` (16 clientes churn de Pauline) que SIEMPRE entran con `manual=True` → badge 📌 y saltan todos los filtros. Asignaciones del remate: ahora también se capturan los cambios de vendedor hechos **en el lead** (el remate del 07-ago se hizo así y no aparecía) y estado "🛒 Compró" si el cliente facturó después de la asignación (caso Ardilla: compró pero nadie movió el CRM).
- **SLA blindado con snapshot de plazos (2026-08-13):** investigando el 100% de SLA se detectó que los 9 preorders de la semana 06-12 ago fueron editados (~22:00, usuario Tomás Vidal) el mismo día de la entrega final — `shipping_date` no tiene tracking (campo base, no se puede activar por ORM: "las propiedades de los campos base no se pueden modificar de esta forma") así que el cambio de fecha es inauditable en Odoo. Pauline habló con Tomás (que no cambie fechas) y pidió "medir realidad". Fix estructural: **`plazos-snapshot.json`** — `extract_operaciones` guarda el `shipping_date` la PRIMERA vez que ve cada pedido y el SLA 24h se evalúa SIEMPRE contra ese plazo original; si la fecha se corre después, el pedido sale "⚠ movido (antes → después)" en Fuera de Plazo, cuenta `prom_movidos` en el KPI de pedidos, y sigue siendo incumplido si la 1ª factura pasó el plazo original. Snapshot podado a 45 días, commiteado por el workflow (`git add crm-data.json plazos-snapshot.json`). Baseline sembrado el 13-ago con 344 pedidos (lo anterior no es reconstruible); detección activa desde la corrida siguiente. Probado: simulación de fecha movida en S34907 → SLA 100→99% y fila marcada. Si se quiere auditoría nativa además del snapshot, pedir al partner del módulo `tracking=True` en `shipping_date`.
- **Cliente Volumen con regla del comité (2026-08-13):** el margen de agosto venía estrecho y el equipo dudaba de `is_volume_client`. Auditoría: el flag lo calculaba `_compute_is_volume_client()` del módulo `cr_average_payment_days` con **promedio anual** (cron 88 diario 01:00, campo readonly) — clientes con crecimiento reciente jamás marcaban (San Pedro 47k L/mes, Tucapel 39k, etc. figuraban retail) y en julio el margen ponderado real fue: volumen 4,0% (meta 6), retail 9,5% (meta 8,5), y los mal clasificados 7,4% arrastrando el bucket retail. **Fix:** cron 88 desactivado; el flag lo calcula ahora el paso 7 de la acción 1243 con la regla del comité (≥20k/mes o ≥5k/despacho en 6m, o proyección anual ≥216k — ver Lógica comercial). Primera corrida: 56 volumen (27 altas, 0 bajas; los 29 del promedio anual también cumplen la regla nueva). Verificado: Duque Sánchez queda retail a propósito (su mes de 40k fue enero, fuera de la ventana de 6m — si molesta, ampliar ventana). Detectadas de pasada fichas duplicadas que diluyen litros (Cancino ×2, Tucapel "S A"/"S.A.", Talma/Talmac) — pendiente fusionar. Los dashboards toman el cambio solo en el próximo refresh (leen `is_volume_client`): el split retail/volumen del margen se va a mover.
- **Dashboard Costos por Camión (2026-08-17, NUEVO):** `costos.html` + `extract_costos.py` (→ `costos-data.json`) + `presupuesto-camiones.json` (manual). Gasto real por centro de costo desde el plan analítico "Camiones" de Odoo (7 cuentas analíticas = 7 patentes; líneas desde dic-2025) vs presupuesto. **Fuentes del presupuesto:** hoja "Presupuesto Operaciones 2026" de `Proyectos y Control.xlsx` (Drive id `1ADhyzQ3soonP-oUp-cosx9-EfaFvI6QM`) — su fila Total (fijos seguros/sitios/TAG/petróleo-7$/L + variable por camión y camioneta) ES la línea "Transporte" aprobada por directorio en "Plan y Presupuesto 2026" (Drive id `1lhqv5r4unqbHOeb...`, hoja "APROBACION DIRECTORIO margenes "), verificado al peso jun–dic (ene–may difiere ±100k). La hoja "MB Presupuesto..." NO es la aprobada. **Cuentas GL de flota:** 3.1.01.02 petróleo, 3.1.01.04/07 seguros, 3.1.01.08+3.3.01.18+3.3.01.29 mantención, 3.3.01.28 arriendo sitios, 3.3.01.52 traslados (los TAG/peajes van AQUÍ, glosa "Peaje <patente>" — no existe cuenta TAG). Clasificación por glosa: neumáticos/hermeticidad/rev.técnica/etc. Comparación por camión = solo variable (excl. petróleo/TAG/seguros/sitios que se presupuestan a nivel flota); flota = GL total vs fijos+variable, con línea plan directorio (incluye camionetas, que no son centros de costo en Odoo). ⚠ Hallazgos: 23% del gasto GL YTD ($27M) SIN distribución analítica (sección propia en el dashboard; pedir a contabilidad asignar camión); TYDG-23 gasta desde may-2026 pero no tiene presupuesto en la planilla; abril salió 173% por trabajos de taller/recertificación (asiento 20-abr). Validación integrada en el extract (gasto YTD ≥$10M, ≥5 camiones, si no exit 1 sin escribir) + checks en `validate_data.py` + workflow `update-costos.yml` (cron 08:30 UTC = 04:30 Chile, escalonado tras los otros 3). **Ajustes del 17-ago (pedido de Pauline):** km/mes por camión (fila "Kilometraje Promedio" de la hoja de presupuesto) con KPI $/km total y variable en cada tarjeta; Gantt de vencimientos por camión (hoja "Vencimientos" del mismo xlsx → `eventos` en `presupuesto-camiones.json`; rojo = vencido, ej. extintores VDKT-95 vencidos 01-07); y **presupuesto ESTIMADO para TYDG-23** (no está en la planilla; baseline $100k/mes imprevistos por garantía Hino + pauta $165k trimestral + hermeticidad/RT oct de Vencimientos ≈ $1,52M may–dic, editable en el JSON, badge "ESTIMADO" en el dashboard; Perm.Circ/SOAP vencen 30-09-2027, $0 en 2026). Al actualizar el presupuesto anual o los vencimientos: regenerar `presupuesto-camiones.json` desde las 2 planillas Drive.
- **Reporte chofer (2026-07-10):** `reporte_chofer_conectado.html` funcionando. Se había roto al cambiar el envío a form submit/iframe (el `doPost` hace `JSON.parse(e.postData.contents)` y el form manda `payload=...` url-encoded → explotaba) y al apuntar a un deployment equivocado. Revertido a `fetch` no-cors + body JSON al deployment activo `AKfycbyEEI...`, con `otro_camion` en el payload. Ver detalle en la sección de archivos.
- Backlog: filtro "sin gestión" en Recuperación · asignar ejecutivo desde dashboard (Opción C browser-login vs D serverless, sin decidir) · export CSV recuperación · cron de durmancia en Odoo · fix `dias_facturacion` · orden de tags (workaround prefijos ZONA-) · **hacer el `doPost` del webhook chofer a prueba de balas** (que lea tanto body JSON como `e.parameter.payload` sin romperse).
